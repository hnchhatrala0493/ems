import{Schema,model}from'mongoose';const schema=new Schema({organizationId:{type:String,index:true},attendanceId:{type:Schema.Types.ObjectId,ref:'Attendance',required:true},employeeId:{type:Schema.Types.ObjectId,ref:'Employee',required:true,index:true},requestedCheckIn:Date,requestedCheckOut:Date,requestedStatus:String,reason:{type:String,required:true},status:{type:String,enum:['PENDING','APPROVED','REJECTED'],default:'PENDING',index:true},reviewedBy:String,reviewNote:String,reviewedAt:Date},{timestamps:true});export const AttendanceCorrection=model('AttendanceCorrection',schema);

import{Schema,model}from'mongoose';const schema=new Schema({userId:{type:String,required:true,index:true},organizationId:String,expoPushToken:{type:String,required:true,unique:true,index:true},platform:{type:String,enum:['android','ios','web'],required:true},deviceName:String,active:{type:Boolean,default:true},lastSeenAt:{type:Date,default:Date.now}},{timestamps:true});schema.index({userId:1,platform:1});export const PushDevice=model('PushDevice',schema);

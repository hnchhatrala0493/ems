import{Server}from'socket.io';import jwt from'jsonwebtoken';import{env}from'../config/env.js';let io:Server|undefined;export function initSocket(server:any){io=new Server(server,{cors:{origin:env.CLIENT_URL,credentials:true}});io.use((socket,next)=>{try{const payload=jwt.verify(String(socket.handshake.auth.token||''),env.JWT_ACCESS_SECRET)as{sub:string};socket.data.userId=payload.sub;next()}catch{next(new Error('Unauthorized'))}});io.on('connection',socket=>socket.join(`user:${socket.data.userId}`));return io}export const emitNotification=(userId:string,payload:unknown)=>io?.to(`user:${userId}`).emit('notification:new',payload);

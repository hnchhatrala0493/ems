import{Server}from'socket.io';import jwt from'jsonwebtoken';import{env}from'../config/env.js';let io:Server|undefined;export function initSocket(server:any){const origins=env.ALLOWED_ORIGINS.split(',').map(x=>x.trim()).filter(Boolean);if(env.NODE_ENV!=='production')origins.push('http://localhost:5173','http://localhost:3000');io=new Server(server,{cors:{origin:origins,credentials:true}});io.use((socket,next)=>{try{const payload=jwt.verify(String(socket.handshake.auth.token||''),env.JWT_ACCESS_SECRET)as{sub:string};socket.data.userId=payload.sub;next()}catch{next(new Error('Unauthorized'))}});io.on('connection',socket=>socket.join(`user:${socket.data.userId}`));return io}export const emitNotification=(userId:string,payload:unknown)=>io?.to(`user:${userId}`).emit('notification:new',payload);


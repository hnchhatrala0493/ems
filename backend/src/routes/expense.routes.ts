import{Router}from'express';import{authenticate}from'../middleware/auth.js';import*as c from'../controllers/expense.controller.js';export const expenseRoutes=Router();expenseRoutes.use(authenticate);expenseRoutes.get('/',c.list);expenseRoutes.post('/',c.create);expenseRoutes.patch('/:id',c.update);expenseRoutes.patch('/:id/submit',c.submit);expenseRoutes.patch('/:id/action',c.action);expenseRoutes.delete('/:id',c.remove);

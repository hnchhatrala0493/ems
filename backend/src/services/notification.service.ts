import{Notification}from'../models/Notification.js';import{NotificationPreference}from'../models/NotificationPreference.js';import{emitNotification}from'../realtime/socket.js';export async function sendNotification(input:{userId:string;organizationId?:string;event:string;title:string;message:string;module:string;recordId?:string;link?:string;severity?:string}){const pref=await NotificationPreference.findOne({userId:input.userId}).lean();const setting=(pref?.events as any)?.[input.event];if(setting&&setting.inApp===false)return null;const item=await Notification.create({...input,channels:['IN_APP',...(setting?.email?['EMAIL']:[]),...(setting?.push?['PUSH']:[])]});emitNotification(input.userId,item.toObject());return item}

import {z} from 'zod'; import {MASTER_TYPES} from '../models/OrganizationMaster.js'; const fields=z.object({type:z.enum(MASTER_TYPES),name:z.string().trim().min(2).max(100),code:z.string().trim().min(2).max(30).regex(/^[A-Za-z0-9_-]+$/),department:z.string().optional(),departmentHead:z.string().optional(),parentDepartment:z.string().optional(),level:z.string().optional(),grade:z.string().optional(),description:z.string().max(500).optional(),status:z.enum(['ACTIVE','INACTIVE']).default('ACTIVE')}); export const createMasterSchema=z.object({body:fields,query:z.any(),params:z.any()}); export const updateMasterSchema=z.object({body:fields.partial(),query:z.any(),params:z.object({id:z.string()})});

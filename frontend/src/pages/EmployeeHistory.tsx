import {useEffect,useState} from 'react';import {Clock3,Loader2} from 'lucide-react';import {employeeApi,type EmployeeHistoryEvent} from '../services/employees';
export function EmployeeHistory(){const[rows,setRows]=useState<EmployeeHistoryEvent[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{employeeApi.history().then(setRows).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <div><h1 className="text-2xl font-bold">Employee History</h1><p className="mb-5 text-sm text-slate-500">Employee lifecycle and profile changes.</p><div className="card overflow-hidden p-0">{loading?<div className="grid h-48 place-items-center"><Loader2 className="animate-spin text-brand"/></div>:error?<p className="p-6 text-red-600">{error}</p>:<div className="overflow-x-auto"><table className="w-full min-w-[760px]"><thead><tr>{['Date','Employee','Event','Previous','New value','Note'].map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{rows.map((x,i)=><tr key={`${x.employeeId}-${i}`}><td className="text-sm">{new Date(x.effectiveDate).toLocaleString()}</td><td><b>{x.employeeName}</b><p className="text-xs text-slate-500">{x.employeeId}</p></td><td><span className="inline-flex items-center gap-1 rounded-full bg-indigo-50 px-2 py-1 text-xs font-bold text-brand"><Clock3 size={13}/>{x.type.replaceAll('_',' ')}</span></td><td>{x.from||'—'}</td><td>{x.to||'—'}</td><td>{x.note||'—'}</td></tr>)}</tbody></table>{!rows.length&&<p className="p-12 text-center text-slate-500">No employee history found.</p>}</div>}</div></div>}

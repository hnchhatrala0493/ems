import { useState } from 'react'; import { Link } from 'react-router-dom'; import { useForgotPasswordMutation } from '../../services/api';
export function ForgotPassword(){const [email,setEmail]=useState('');const [sent,setSent]=useState(false);const [request,{isLoading}]=useForgotPasswordMutation();return <main className="flex min-h-screen items-center justify-center p-6"><form className="card w-full max-w-md" onSubmit={async e=>{e.preventDefault();await request({email}).unwrap();setSent(true)}}><h1 className="text-2xl font-bold">Forgot your password?</h1><p className="my-3 text-sm text-slate-500">Enter your work email and we’ll send reset instructions if an account exists.</p>{sent?<p className="rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700">Check your email for a password reset link.</p>:<><label className="text-sm font-semibold">Work email</label><input type="email" required className="input my-2" value={email} onChange={e=>setEmail(e.target.value)}/><button className="button mt-3 w-full" disabled={isLoading}>Send reset link</button></>}<Link to="/login" className="mt-5 block text-center text-sm font-semibold text-brand">Back to sign in</Link></form></main>}

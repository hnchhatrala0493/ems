import { useState } from 'react'; import { Link,useSearchParams } from 'react-router-dom'; import { useResetPasswordMutation } from '../../services/api';
export function ResetPassword(){const [params]=useSearchParams();const [password,setPassword]=useState('');const [done,setDone]=useState(false);const [reset,{isLoading}]=useResetPasswordMutation();return <main className="flex min-h-screen items-center justify-center p-6"><form className="card w-full max-w-md" onSubmit={async e=>{e.preventDefault();await reset({token:params.get('token')||'',password}).unwrap();setDone(true)}}><h1 className="text-2xl font-bold">Create a new password</h1><p className="my-3 text-sm text-slate-500">Use at least eight characters and avoid reused passwords.</p>{done?<><p className="rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700">Password changed successfully.</p><Link to="/login" className="button mt-4 w-full">Sign in</Link></>:<><input type="password" minLength={8} required className="input" value={password} onChange={e=>setPassword(e.target.value)} placeholder="New password"/><button className="button mt-4 w-full" disabled={isLoading||!params.get('token')}>Reset password</button></>}</form></main>}

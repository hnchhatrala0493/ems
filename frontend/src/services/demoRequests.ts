export type DemoRequest={_id:string;requestId:string;fullName:string;workEmail:string;phoneCountryCode:string;phoneNumber:string;jobTitle:string;customJobTitle?:string;companyName:string;companyWebsite?:string;industry:string;customIndustry?:string;companySize:string;country:string;state?:string;city?:string;interestedModules:string[];currentHRMethod:string;preferredDemoDate:string;preferredDemoTime:string;preferredTimezone:string;preferredContactMethod:string;additionalRequirements?:string;status:string;priority:string;customerEmailStatus:string;adminEmailStatus:string;confirmedDemoDate?:string;confirmedStartTime?:string;confirmedEndTime?:string;meetingProvider?:string;meetingLink?:string;createdAt:string};
type Envelope<T>={success:boolean;message:string;data:T};const base=import.meta.env.VITE_API_BASE_URL||'/api/v1',token=()=>localStorage.getItem('token')||sessionStorage.getItem('token')||'';
async function request<T>(path:string,init:RequestInit={}){const r=await fetch(`${base}/admin/demo-requests${path}`,{...init,headers:{authorization:`Bearer ${token()}`,'content-type':'application/json',...init.headers}}),body=await r.json().catch(()=>({}));if(!r.ok)throw new Error(body.message||'Request failed');return(body as Envelope<T>).data}
export const demoRequestsApi={list:(query='')=>request<{items:DemoRequest[];pagination:{page:number;limit:number;total:number;pages:number}}>(`?${query}`),stats:()=>request<Record<string,number>>('/stats'),detail:(id:string)=>request<{item:DemoRequest;timeline:Array<{_id:string;action:string;createdAt:string}>}>(`/${id}`),update:(id:string,value:Record<string,unknown>)=>request<DemoRequest>(`/${id}`,{method:'PATCH',body:JSON.stringify(value)}),retry:(id:string,target='both')=>request<DemoRequest>(`/${id}/retry-email`,{method:'POST',body:JSON.stringify({target})}),archive:(id:string,reason:string)=>request(`/${id}/archive`,{method:'PATCH',body:JSON.stringify({reason})})};


import{useState}from'react';import{steps}from'./config';
export function useBookDemoStepper(form){const[current,setCurrent]=useState(0),[completed,setCompleted]=useState(new Set()),[visited,setVisited]=useState(new Set([0]));const go=i=>{setCurrent(i);setVisited(v=>new Set(v).add(i));window.scrollTo({top:0,behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'})};const next=async()=>{const fields=steps[current].fields.filter(x=>!(x==='customJobTitle'&&form.getValues('jobTitle')!=='Other')&&!(x==='customIndustry'&&form.getValues('industry')!=='Other'));if(!await form.trigger(fields,{shouldFocus:true}))return false;setCompleted(v=>new Set(v).add(current));go(Math.min(current+1,4));return true};return{current,completed,visited,next,previous:()=>go(Math.max(0,current-1)),go}};
